# Алгоритм скрещивания перестановок

## 1. Основные определения

Пусть даны две перестановки длины $n$:  
$a = (a_1,\dots,a_n)$, $b = (b_1,\dots,b_n)$.

**Множество совпадений** $R(a,b)$ — индексы, в которых перестановки совпадают:
$$R(a,b) = \{i \mid a_i = b_i\}.$$

Для дочерней перестановки $c$ определяем:
- **Множества наследуемых признаков**: $R(a,c)$ (от $a$) и $R(b,c)$ (от $b$);
- **Множество мутаций**: индексы, не попавшие ни в одно из наследуемых множеств:
  $$M(a,b,c) = \{i \mid c_i \neq a_i \text{ и } c_i \neq b_i\}.$$

**Равномерное скрещивание** — скрещивание, при котором мощности наследуемых признаков отличаются не более чем на $1$:
$$-1 \le |R(a,c)| - |R(b,c)| \le 1.$$

## 2. Задача о равномерном скрещивании без мутаций

**Дано:** две родительские перестановки $a$ и $b$ длины $n$.  
**Требуется:** найти перестановку $c$ такую, что скрещивание равномерно и множество мутаций пусто: $M(a,b,c) = \varnothing$.

Для упрощения считаем, что $a_i \neq b_i$ для всех $i$ (если есть совпадения, их принудительно наследуют и исключают из рассмотрения).

## 3. Теорема

Пусть $a$ и $b$ — две перестановки длины $n$, не совпадающие ни в одной из позиций, 
а $c_1, \dots, c_k$ — представление перестановки $b a^{-1}$ в виде произведения независимых циклов. Обозначим длины этих циклов через $\ell_1, \dots, \ell_k$ (так что $\sum_{j=1}^k \ell_j = n$).

**Утверждение теоремы.**
1. **Строго равномерное скрещивание** (при котором $|R(a,c)| = |R(b,c)| = n/2$ и $M(a,b,c) = \varnothing$)  
   существует **тогда и только тогда**, когда $n$ чётно и найдётся такое подмножество индексов $J \subseteq \{1,\dots,k\}$, что  
   $$
   \sum_{j \in J} \ell_j = \frac{n}{2}.
   $$

2. **Общее равномерное скрещивание** (при котором $-1 \le |R(a,c)| - |R(b,c)| \le 1$ и $M(a,b,c) = \varnothing$)  
   существует **тогда и только тогда**, когда найдётся подмножество $J \subseteq \{1,\dots,k\}$, для которого  
   $$
   \sum_{j \in J} \ell_j = \left\lfloor \frac{n}{2} \right\rfloor
   \quad \text{или} \quad
   \sum_{j \in J} \ell_j = \left\lceil \frac{n}{2} \right\rceil.
   $$

## 4. Доказательство

### 4.1. Сведение к единичной перестановке
Умножим обе родительские перестановки справа на $a^{-1}$:
- Первая превращается в единичную $e = (1,\dots,n)$,
- Вторая — в $p = b a^{-1}$.

Для искомого потомка $c$ определим $q = c a^{-1}$. Тогда:
$$c_i = a_i \iff q_i = i \quad \text{(наследование от a)},$$
$$c_i = b_i \iff q_i = p_i \quad \text{(наследование от b)}.$$

Мощности наследуемых множеств:
$$|R(a,c)| = |\{i \mid q_i = i\}|,\quad |R(b,c)| = |\{i \mid q_i = p_i\}|.$$
Так как $a_i \neq b_i$, имеем $i \neq p_i$; следовательно, множества $\{i \mid q_i=i\}$ и $\{i \mid q_i=p_i\}$ не пересекаются.

### 4.2. Критерий того, что $q$ — перестановка
Пусть $S = \{i \mid q_i = i\}$. Тогда
$$q_i = \begin{cases}
i, & i \in S,\\
p_i, & i \notin S.
\end{cases}$$

**Утверждение.** $q$ является биекцией $\iff$ $S$ инвариантно относительно $p$ ($p(S)=S$) $\iff$ $S$ — объединение целых циклов перестановки $p$.

**Достаточность.** Если $S$ — объединение циклов, то $p$ отображает $S$ на $S$, а $T = \{1,\dots,n\}\setminus S$ на $T$. На $S$ функция $q$ тождественна, на $T$ совпадает с сужением $p$ — оба отображения биективны, значит, и $q$ биекция.

**Необходимость.** Пусть $q$ — биекция. Предположим $\exists x \in S$ с $p(x) \notin S$, т.е. $p(x) \in T$. Рассмотрим $y = p^{-1}(x)$. Если $y \notin S$, то $q(y) = p(y) = x$. Тогда у $x$ появляются два прообраза: $x$ (из $S$, т.к. $q(x)=x$) и $y$ (из $T$). Это противоречит инъективности $q$. Следовательно, $p(S) \subseteq S$, а из конечности множеств $p(S) = S$.

Таким образом, $S$ обязано быть объединением некоторых циклов $p$.

### 4.3. Связь с равномерностью
$S$ — индексы наследования от $a$, $T = \{1,\dots,n\}\setminus S$ — от $b$, поэтому
$$|R(a,c)| = |S|,\qquad |R(b,c)| = n - |S|.$$
$M = \varnothing$ выполнено автоматически.

- **Строгая равномерность:** $|S| = n/2$, что возможно только при чётном $n$ и даёт сумму длин циклов в $S$ равную $n/2$.
- **Общая равномерность:** требуется $|S| \in \{\lfloor n/2\rfloor, \lceil n/2\rceil\}$, что эквивалентно сумме длин циклов $\lfloor n/2\rfloor$ или $\lceil n/2\rceil$.

### 4.4. Построение $c$
Если нужное подмножество циклов $J$ найдено, полагаем $S = \bigcup_{j\in J}C_j$. Строим $q$ по правилу выше; $q$ — биекция. Тогда искомая перестановка $c = q a$ даёт $c_i = a_i$ при $i \in S$ и $c_i = b_i$ при $i \notin S$, т.е. равномерное скрещивание без мутаций.

## 5. Решение (детерминированное)
Как показано выше, задача сводится к поиску подмножества циклов перестановки $p = b a^{-1}$ с нужной суммой длин.  
Алгоритм:
1. Вычислить $p = b a^{-1}$.
2. Разложить $p$ на циклы $C_1,\dots,C_k$.
3. Найти подмножество циклов с суммой длин $\lfloor n/2\rfloor$ или $\lceil n/2\rceil$ (для чётного $n$ — ровно $n/2$). Если такого нет — идеального решения не существует.
4. Положить $S = \bigcup_{j\in J} C_j$, $q_i = i$ для $i\in S$, $q_i = p_i$ для $i\notin S$.
5. Итоговая перестановка: $c = q a$.

## 6. Скрещивание с рандомизацией (ограничение мутаций)

Если точное равномерное решение невозможно или требуется внести контролируемые мутации, используется следующий вероятностный алгоритм.

**Параметр:** $k$ — допустимое количество дополнительных мутаций (фактически число мутаций не превзойдёт $k+1$).  
**Вход:** $a$, $b$, $k$. Полагаем $p = a b^{-1}$.

Число признаков, наследуемых от $a$, будет примерно $m = (n - k)/2$ (если не целое — округляется подходящим образом).

### 6.1. Первая часть — набор индексов
- Изначально множество выбранных индексов $S = \varnothing$, $j = -1$, текущий запас прыжков $K = k$.
- Повторять $m$ раз:
  1. Если $j = -1$ **или** $p[j] \in S$ **или** (random($0, m$) $< K$):
     - если random($0, m$) $< K$, то $K \leftarrow K - 1$;
     - выбрать случайный индекс $j$ из ещё не выбранных;
     - добавить $p[j]$ в $S$;
  2. Иначе: $j \leftarrow p[j]$.
  3. Добавить $j$ в $S$.

### 6.2. Вторая часть — построение $q$
1. Разбить выбранные индексы $S$ на цепочки в порядке их обхода во время набора.
2. Каждую цепочку замкнуть в цикл (последний элемент указывает на первый).
3. Все не попавшие в $S$ индексы оставить неподвижными точками.
4. Полученная перестановка $q$ задаёт скрещивание: $c = q b$ (или в зависимости от стороны умножения, так чтобы $c$ совмещало наследование от $a$ и $b$).
